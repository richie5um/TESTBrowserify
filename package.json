{
  "name": "testbrowserify",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "node_modules/.bin/browserify src/index.js --standalone beep > dist/index.js",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "R. Somerfield",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.0.1"
  }
}
